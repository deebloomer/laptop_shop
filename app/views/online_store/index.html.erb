<!--<h1>OnlineStore#index</h1>-->
<!--<p>Find me in app/views/online_store/index.html.erb</p>-->

<h1>Listing Routes</h1>         <!-->dee -->

<table>
  <tr>
    <th>Outbound</th>
    <th>Start</th>
    <th>Finish</th>
    <th>Return</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

  <% @routes.each do |route| %>
      <tr>
        <td><%= route.outbound_code %></td>
        <td><%= route.start %></td>
        <td><%= route.finish %></td>
        <td><%= route.inbound_code %></td>
        <td><%= link_to 'Show', product %></td>

        <% if current_user and current_user.admin %>
            <td><%= link_to 'Edit', edit_product_path(product) %></td>
            <td><%= link_to 'Destroy', product, method: :delete, data: {confirm: 'Are you sure?'} %></td>
            <td><%= button_to 'Add to Cart', line_items_path(:product_id => product) %></td>
            </tr>
        <% end %>

  <% end %>
</table>